// The favicon bytes served at /favicon.ico and /favicon.png. Re-exported from
// the shared src/ source of truth (also used for the MCP serverInfo icon), so
// the Finlens logo is defined in exactly one place.
export { FINLENS_ICON_PNG as FAVICON_PNG } from "../../src/server/finlens-icon.js";
